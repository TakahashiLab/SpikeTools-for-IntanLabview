# Legacy MATLAB Entry Points

This folder contains older MATLAB functions that previously lived in the
repository root.

They were moved here to keep the top-level directory easier to scan. To keep
existing function calls working, run this from the repository root before using
the toolbox:

```matlab
setup_spiketools_path
```
